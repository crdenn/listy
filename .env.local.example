# ===========================================
# Firebase Configuration
# ===========================================
# Get these values from your Firebase Console:
# 1. Go to console.firebase.google.com
# 2. Select your project (or create one)
# 3. Click the gear icon → Project Settings
# 4. Scroll to "Your apps" section
# 5. Click the web app (</>) icon to add a web app if you haven't
# 6. Copy the config values from the Firebase SDK snippet

NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key-here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# ===========================================
# Application Settings
# ===========================================
# The base URL of your application (used for generating share links)
# For local development: http://localhost:3000
# For production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Server-side product ingestion
# ===========================================
# Used by the product ingestion API to verify users and call external enrichers.
# Firebase Admin credentials (service account) are required for secure token verification.
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional enrichers (leave blank to disable a given provider)
DIFFBOT_TOKEN=
BRIGHTDATA_API_KEY=
BRIGHTDATA_AMAZON_DATASET_ID=gd_l7q7dkf244hwjntr0
BRIGHTDATA_WALMART_DATASET_ID=gd_l95fol7l1ru6rlo116

# ===========================================
# Optional: Development vs Production
# ===========================================
# Set to 'development' or 'production'
# This can be used to switch between Firebase projects
NODE_ENV=development
